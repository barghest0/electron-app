import styled from 'styled-components';

import { container } from 'shared/styles/mixins';

const Main = styled.div`
  padding: 20px 0;
`;

const Container = styled.div`
  ${container}
`;

const Form = styled.form``;

export { Main, Container, Form };
